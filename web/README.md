# DevConnect Web

Frontend for the DevConnect course project (Vite + React + Tailwind).

## Getting Started
```bash
cd web
npm install
npm run dev
```

## Env
If you add client-side env vars, put them in `.env` and document them in `.env.example`.
